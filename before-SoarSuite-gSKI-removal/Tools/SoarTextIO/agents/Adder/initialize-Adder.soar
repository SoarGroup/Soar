sp {propose*initialize-Adder
   (state <s> ^superstate nil
             -^name)
-->
   (<s> ^operator <o> +)
   (<o> ^name initialize-Adder)
}

sp {apply*initialize-Adder
   (state <s> ^operator <op>)
   (<op> ^name initialize-Adder)
-->
   (<s> ^name Adder)
}

