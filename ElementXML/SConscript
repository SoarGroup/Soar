#!/usr/bin/python
import glob
import sys

Import('env')
env = env.Clone()
env.Prepend(CPPPATH = ['src', 'include',])

if env['scu']:
	ElementXML = env.SharedLibrary('ElementXML', 'ElementXML.cxx')
else:
	ElementXML = env.SharedLibrary('ElementXML', glob.glob('src/*.cpp'))

env.Install('#SoarLibrary/lib', ElementXML)

