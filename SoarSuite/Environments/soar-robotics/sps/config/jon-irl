include = common;

all = [control, splinter];

env {
	environment = custom_environment;
}

custom_environment {
	EXAMPLE_VAR = "indeed";
}

control_config {
	productions = "agents/jon-agent.soar";	
}
