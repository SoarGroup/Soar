sp {propose*initialize-selection
   (state <s> ^superstate nil
             -^name)
-->
   (<s> ^operator <o> +)
   (<o> ^name initialize-selection)
}

sp {apply*initialize-selection
   (state <s> ^operator <op>)
   (<op> ^name initialize-selection)
-->
   (<s> ^name selection)
}

