#!/usr/bin/python
import os
import SoarSCons

Import('env')
env = env.Clone()

theJar = '#Tools/SoarJavaDebugger/SoarJavaDebugger.jar';
sources = SoarSCons.JavaSources('src')

Depends(theJar, '#Core/ClientSMLSWIG/Java/sml.jar')
Depends(theJar, 'build.xml')

env.Command(theJar, sources, 'ant', chdir=1)

