sp {jon-agent*propose*done
   (state <s> ^name jon-agent
              ^io.input-link.time.seconds >= 120)
-->
   (<s> ^operator <op> + >)
   (<op> ^name done)
}

sp {apply*done
   (state <s> ^operator <op>)
   (<op> ^name done)
-->
   (write (crlf) |Done.|)
   (halt)
}
