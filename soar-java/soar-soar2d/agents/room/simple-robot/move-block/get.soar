sp {wander*propose*get
   (state <s> ^name move-block
              ^top-state.block-id <bid>
             -^io.input-link.self.carry.id <bid>)
-->
   (<s> ^operator <o> + =)
   (<o> ^name get
        ^actions.get.id <bid>
        ^actions.move.direction stop
        ^actions.rotate-relative.yaw 1.57)
}
