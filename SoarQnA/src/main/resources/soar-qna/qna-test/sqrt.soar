sp {qna-test*propose*sqrt
   (state <s> ^name qna-test
              ^test <t>
              ^io.input-link.qna-registry.math.query sqrt)
   (<t> ^source math
        ^query sqrt
       -^done)
-->
   (<s> ^operator <op> + =)
   (<op> ^name sqrt)
}
