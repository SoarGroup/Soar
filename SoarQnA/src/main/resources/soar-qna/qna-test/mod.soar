sp {qna-test*propose*mod
   (state <s> ^name qna-test
              ^test <t>
              ^io.input-link.qna-registry.math.query mod)
   (<t> ^source math
        ^query mod
       -^done)
-->
   (<s> ^operator <op> + =)
   (<op> ^name mod)
}
