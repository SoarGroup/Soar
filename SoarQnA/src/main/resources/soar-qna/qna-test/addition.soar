sp {qna-test*propose*addition
   (state <s> ^name qna-test
              ^test <t>
              ^io.input-link.qna-registry.math.query |+|)
   (<t> ^source math
        ^query |+|
       -^done)
-->
   (<s> ^operator <op> + =)
   (<op> ^name addition)
}
