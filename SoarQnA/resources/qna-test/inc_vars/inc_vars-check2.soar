sp {inc_vars*propose*inc_vars-check2
   (state <s> ^name inc_vars
              ^superstate.io.output-link.qna-next)
-->
   (<s> ^operator <op> + =)
   (<op> ^name inc_vars-check2)
}

sp {apply*inc_vars-check2
   (state <s> ^operator <op>
              ^superstate <ss>)
   (<ss> ^test <t>
         ^io.output-link <out>)
   (<t> ^source test_db
        ^query all_vars
        ^num two)
   (<op> ^name inc_vars-check2)
   (<out> ^qna-next.status complete
          ^qna-query.result.next <n>)
   (<n> ^features.key 9
        ^next pending)
-->
   (<t> ^done true)
   (write |inc_vars: success|)
}
