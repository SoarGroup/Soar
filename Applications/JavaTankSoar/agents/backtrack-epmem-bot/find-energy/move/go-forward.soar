sp {move*propose*go-forward
   (state <s> ^name move
              ^direction <abs-dir>
              ^top-state.coord.forward <abs-dir>)
-->
   (<s> ^operator <o> + =)
   (<o> ^name go-forward
        ^actions.move.direction forward)
}


