${createdComment}

sp {elaborate*top-state*name
  (state <s> ^superstate nil)
-->
  (<s> ^name ${project})
}

sp {elaborate*top-state*top-state
  (state <s> ^name ${project})
-->
  (<s> ^top-state <s>)
}

