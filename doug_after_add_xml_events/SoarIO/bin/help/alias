   Link: Creative Commons (meta)
   Link: copyright

                                     Alias

  From Soar Wiki

   This is part of the Soar Command Line Interface.

    Table of contents
   1 Name
   2 Synopsis
   3 Options
   4 Description
   5 Examples
   6 See Also
   7 Structured Output

   7.1 On Success
   7.2 Notes

   8 Error Values

   8.1 During Parsing
   8.2 During Execution

   [edit]

                                      Name

   alias - Define a new alias, or command, using existing commands and
   arguments.
   Status: Complete

   [edit]

                                    Synopsis

 alias name [cmd <args>]
 alias -d name
 alias

   [edit]

                                    Options

   +------------------------------------------------------------------------+
   |-d, --disable, --off|Remove the named alias.                            |
   |--------------------+---------------------------------------------------|
   |name                |The name of the alias, i.e. the new command.       |
   |--------------------+---------------------------------------------------|
   |cmd                 |An existing command that will be invoked when the  |
   |                    |alias is entered on the commandline.               |
   |--------------------+---------------------------------------------------|
   |args                |Valid arguments to the cmd (optional & optional    |
   |                    |number).                                           |
   +------------------------------------------------------------------------+

   [edit]

                                  Description

   This command defines new aliases by creating Soar procedures with the
   given name. The new procedure can then take an arbitrary number of
   arguments which are post-pended to the given definition and then that
   entire string is executed as a command. The definition must be a single
   command,multiple commands are not allowed. The alias procedure checks to
   see if the name already exists, and does not destroy existing procedures
   or aliases by the same name. Existing aliases can be removed by using the
   -d flag. With no arguments, alias returns the list of defined aliases.
   With only the name given, alias returns the current definition.

   [edit]

                                    Examples

   The alias wmes is defined as:

 alias wmes print -depth 0 -internal

   If the user executes a command such as:

           wmes {(* ^superstate nil)}

   it is as if the user had typed this command:

           print -depth 0 -internal {(* ^superstate nil)}

   [edit]

                                    See Also

   unalias

   [edit]

                               Structured Output

   [edit]

On Success

 <result>
   <arg name="alias" type="string">alias</arg>
   <arg name="aliasedcommand" type="string">aliased_command</arg>
 </result>

   [edit]

Notes

     * When listing the current alias database, each alias will have the
       alias/aliased command pair returned.
     * An example pair for step (run -s 1) would be:

 <arg name="alias" type="string">step</arg>
 <arg name="aliasedcommand" type="string">run -s 1</arg>

   [edit]

                                  Error Values

   [edit]

During Parsing

   kMissingOptionArg, kUnrecognizedOption, kGetOptError, kTooManyArgs,
   kTooFewArgs

   [edit]

During Execution

   kAliasNotFound, kAliasExists

   Retrieved from "http://winter.eecs.umich.edu/soarwiki/Alias"

      Views

     * Article
     * Discussion
     * Edit
     * History

      Personal tools

     * Create an account or log in

      Navigation

     * Main Page
     * Community portal
     * Current events
     * Recent changes
     * Random page
     * Help

      Search

   _____________________ [ Go ]   [ Search ]

      Toolbox

     * What links here
     * Related changes
     * Special pages

   MediaWiki
   GNU Free Documentation License 1.2

     * This page was last modified 18:57, 23 Mar 2005.
     * This page has been accessed 39 times.
     * Content is available under GNU Free Documentation License 1.2.
     * About Soar Wiki
     * Disclaimers
