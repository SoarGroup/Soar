bots = 1                         # required
host = winter.eecs.umich.edu

name0 = zero                      # optional, defaults to robotN where n is array index
productions0 = ../player-client/agents/simple-robot-replanner.soar # required
port0 = 6665                     # optional, defaults to (6666 + N) where n is array index
