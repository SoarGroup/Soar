sp {propose*initialize-simple-robot
   (state <s> ^superstate nil
             -^name)
-->
   (<s> ^operator <o> +)
   (<o> ^name initialize-simple-robot)
}

sp {apply*initialize-simple-robot
   (state <s> ^operator <op>)
   (<op> ^name initialize-simple-robot)
-->
   (<s> ^name simple-robot
        ^action-interval-time 3) # how much time the agent should wait (as reported on the input-link) between actions
}

