sp {go-to-waypoint*elaborate*state*destination-waypoint
   (state <s> ^name go-to-waypoint
              ^io.input-link.self.waypoints.waypoint <w>
              ^superoperator.destination <d>)
   (<w> ^id destination ^x <x> ^y <y>)
   (<d> ^x <x> ^y <y>)
-->
   (<s> ^waypoint <w>)
}