sp {propose*initialize
   (state <s> ^superstate nil
             -^name)
-->
   (<s> ^operator <o> + >)
   (<o> ^name initialize)
}

sp {apply*initialize
   (state <s> ^operator <op>)
   (<op> ^name initialize)
-->
   (<s> ^name simple-robot
        ^parameters <p>)
   (<p> ^throttle 0.5)
}

