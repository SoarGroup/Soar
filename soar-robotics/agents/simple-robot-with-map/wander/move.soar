sp {wander*propose*move
   (state <s> ^name wander
              ^next-action-time <nat>
              ^io.input-link.time.seconds >= <nat>)
-->
   (<s> ^operator <o> + =)
   (<o> ^name move
        ^actions.move <m>)
   (<m> ^direction forward
        ^throttle 1.0)}