bots = 2                         # required

name0 = one                      # optional, defaults to robotN where n is array index
productions0 = agents/simple-robot-replanner.soar # required
port0 = 6666                     # optional, defaults to (6666 + N) where n is array index

name1 = two
productions1 = agents/simple-robot-replanner.soar
port1 = 6667
