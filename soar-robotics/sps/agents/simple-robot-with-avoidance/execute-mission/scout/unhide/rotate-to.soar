sp {unhide*elaborate*state*desired-yaw
   (state <s> ^superoperator <so>
              ^io.input-link.self.pose <p>)
   (<so> ^name unhide
         ^waypoint <w>)
   (<p> ^x <x1> ^y <y1>)
   (<w> ^x <x2> ^y <y2>)
-->
   (<s> ^desired-yaw (compute-heading <x1> <y1> <x2> <y2>))
}

sp {unhide*elaborate*superoperator*move-now
   (state <s> ^name unhide
              ^parameters.rotation-tolerance <rt>
              ^yaw-diff <= <rt>
              ^superoperator <so>)
-->
   (<so> ^move-now true)
}