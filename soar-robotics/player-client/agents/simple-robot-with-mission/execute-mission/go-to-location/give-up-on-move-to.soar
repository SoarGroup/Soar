sp {simple-robot*elaborate*move-to-cutoff-time
   (state <s> ^name go-to-location
              ^superoperator.move-to <mt>
              ^top-state.replan-time <rpt>)
   (<mt> ^time <t>)
-->
   (<mt> ^cutoff (+ <t> <rpt>))
}

sp {simple-robot*elaborate*move-to-give-up
   (state <s> ^name go-to-location
              ^superoperator.move-to <mt>
              ^io.input-link.time.seconds > <ct>)
   (<mt> ^cutoff <ct>)
-->
   (<mt> ^give-up true)
}

sp {simple-robot*propose*give-up-on-move-to
   (state <s> ^name go-to-location
              ^superoperator.move-to <mt>)
   (<mt> ^give-up true)
-->
   (<s> ^operator <op> +)
   (<op> ^name give-up-on-move-to
         ^move-to <mt>)
}



sp {apply*give-up-on-move-to
   (state <s> ^operator <op>
              ^waypoints.at <w>
              ^superoperator <so>)
   (<op> ^name give-up-on-move-to
         ^map-updated true
         ^move-to <mt>)
   (<so> ^move-to <mt>)
   (<mt> ^source <w>)
-->
   (<so> ^move-to <mt> -)
}
