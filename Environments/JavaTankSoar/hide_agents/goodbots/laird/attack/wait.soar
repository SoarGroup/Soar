echo "\nLoading wait/slide.soar "

## Wait until time to slide
sp {attack*propose*wait  
   (state <s> ^saved-slide-direction <d>
             -^operator.name wait-for-slide)
   -->
   (<s> ^operator <o> + =, <)
   (<o> ^name wait-for-slide ^type wait)}


sp {top-ps*apply*wait*random
   (state <s> ^operator <o>)
   (<o> ^name wait)
-->
   (<o> ^random elaboration)}




