echo "\nLoading wander/move.soar "

# Propose Move-forward operator if front is not blocked
# If the tank is not blocked in the forward direction, propose the move 
# operator.
#NOTE:  quiescence t is prevented to prevent chunking.  Since we are wandering randomly
#       chunking would create a tank that always wanders to the same places.

sp {wander*propose*move
   (state <s> ^name wander
              ^io.input-link.blocked.forward no
              ^quiescence t)
-->
   (<s> ^operator <o> + =)
   (<o> ^name move
        ^actions.move.direction forward)
}
