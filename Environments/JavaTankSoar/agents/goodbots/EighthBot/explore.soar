echo "\nLoading explore.soar"

### TEMPORARY RULE TO FORCE EXPLORATION ONLY
#sp {propose*explore*always
#   (state <s> ^name tanksoar
#              ^status <st>)
#-->
#   (<s> ^operator <o> +  >,=)
#   (<o> ^name explore)
#}

# Propose EXPLORE operator
#    only 'explore' if resources are OK and no sign of the enemy
#
sp {propose*explore
   (state <s> ^name tanksoar
              ^status <st>)
   (<st> -^in-danger yes
         -^enemy-detected
         -^enemy-sighted
         -^need-health yes
         -^need-energy yes
         -^need-missiles yes)
-->
   (<s> ^operator <o> + =)
   (<o> ^name explore)
}
