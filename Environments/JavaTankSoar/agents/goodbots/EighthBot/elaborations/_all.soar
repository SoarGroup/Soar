sp {elaborate*state*copy*name
  (state <s> ^superstate.operator.name <name>)
-->
  (<s> ^name <name>)
}

sp {elaborate*state*copy*top-state
  (state <s> ^superstate.top-state <ts>)
-->
  (<s> ^top-state <ts>)
}

sp {elaborate*state*copy*io
   (state <s> ^superstate.io <io>)
-->
   (<s> ^io <io>)}


# copy parameter pointer to each new state
sp { elaborate*state*copy*parameters
   (state <s> ^superstate.parameters <p>)
-->
   (<s> ^parameters <p>)}

# copy status pointer to each new state
sp { elaborate*state*copy*status
   (state <s> ^superstate.status <p>)
-->
   (<s> ^status <p>)}

# copy direction-map pointer to each new state
sp { elaborate*state*copy*direction-map
   (state <s> ^superstate.direction-map <p>)
-->
   (<s> ^direction-map <p>)}


# copy map pointer to each new state
sp { elaborate*state*copy*map
   (state <s> ^superstate.map <p>)
-->
   (<s> ^map <p>)}

# copy string-to-integer conversion structure
sp {elaborate*state*copy*string-to-integer
   (state <s> ^superstate.string-to-integer <p>)
-->
   (<s> ^string-to-integer <p>)
}

# copy opposite-map pointer to each new state
sp { elaborate*state*copy*opposite-direction
   (state <s> ^superstate.opposite-direction <p>)
-->
   (<s> ^opposite-direction <p>)}