echo "\nLoading common/monitor.soar"

#### Monitor impasses I don't expect to get

sp {top-ps*monitor*impasse*state-no-change
   (state <s> ^attribute state
              ^choices none)
-->
   (write (crlf) | State no-change impasse detected.|)
}

sp {top-ps*monitor*operator*tie*impasse
   (state <s> ^attribute operator
              ^impasse tie
              ^item <i1> ^item {<i2> <> <i1> })
   (<i1> ^name <name1>)
   (<i2> ^name <name2>)
-->
   (write (crlf) | Operator tie between | <name1> | & | <name2>)}

