echo "\nLoading wander/charge.soar "
################################
### Propose wait if on charger
################################

sp {wander*propose*wait*charge
   (state <s> ^name wander
              ^charger-ready yes
             -^operator.name charge)
-->
   (<s> ^operator <o> +)
   (<o> ^name charge
        ^type wait)}
