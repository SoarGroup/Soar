#echo "\nLoading all/shields.soar "

sp {elaborate*shields-on
   (state <s> ^name << wander retreat chase >>
              ^operator.actions <a>
              ^io.input-link <il>)
   (<il> ^incoming.<dir> yes
         ^shield-status off)
   -->
   (<a> ^shields.switch on)}

sp {elaborate*shields-off
   (state <s> ^name << wander retreat chase >>
              ^operator.actions <a>
              ^io.input-link <il>)
   (<il> -^incoming.<dir> yes
          ^shield-status on)
   --> 
   (<a> ^shields.switch off)}
