sp {move-to-gateway*propose*rotate
   (state <s> ^name move-to-gateway
              ^need-to-turn true
             -^moving true
             -^rotating true
              ^gateway.center.angle-off.yaw <ao>)
-->
   (<s> ^operator <op> +)
   (<op> ^name rotate
         ^actions.rotate-relative.yaw <ao>)
         #^actions.rotate-relative.yaw (* -1.0 <ao>))
}

sp {monitor*rotate-relative
   (state <s> ^io.output-link.rotate-relative.yaw <a>)
-->
   (write (crlf) |Rotating relative: | <a>)
}
