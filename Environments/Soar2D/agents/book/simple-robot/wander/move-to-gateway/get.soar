sp {wander*propose*get
   (state <s> ^name move-to-gateway)
   (<s> ^top-state.io.input-link.object <obj>)
   (<obj> ^visible yes
        ^range < 16.0
        ^id <id>)
-->
   (<s> ^operator <o> + =)
   (<o> ^name get
        ^actions.get.id <id>)
}
