sp {wander*propose*move-to-gateway
   (state <s> ^name book
             -^block-id)
   (<s> ^io.input-link.self.area-description <ad>)
   (<ad> ^gateway <g>)
-->
   (<s> ^operator <o> + =)
   (<o> ^name move-to-gateway
        ^gateway <g>)
}