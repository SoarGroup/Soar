sp {get-block*propose*pick-up-block
   (state <s> ^name get-block
              ^block.id <id>
              ^aligned row
              ^aligned col)
-->
   (<s> ^operator <op> + >)
   (<op> ^name pick-up-block
         ^actions.get.id <id>)
}

