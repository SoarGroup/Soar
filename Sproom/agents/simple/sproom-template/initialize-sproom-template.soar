sp {propose*initialize-sproom-template
   (state <s> ^superstate nil
             -^name)
-->
   (<s> ^operator <o> +)
   (<o> ^name initialize-sproom-template)
}

sp {apply*initialize-sproom-template
   (state <s> ^operator <op>)
   (<op> ^name initialize-sproom-template)
-->
   (<s> ^name sproom-template)
}

