sp {execute-mission*propose*circuit-patrol
   (state <s> ^name execute-mission
              ^top-state.current-mission.type << linear-patrol circuit-patrol >> )
-->
   (<s> ^operator <op> + =)
   (<op> ^name patrol)
}

sp {apply*circuit-patrol*set-next-area-id
   (state <s> ^operator.name patrol
              ^top-state.current-mission <cm>)
   (<cm> -^next-area
          ^first-area <area>)
-->
   (<cm> ^next-area <area>)
}
