# NCC tests

sp {apply19
    (state <s> ^name substate
               ^operator.name test
               ^superstate <ss>)
    (<ss> ^test-item { < <max> <min>}
          ^test-item <max>)
         -{(<ss> ^test-item 20)}
-->
    (<ss> ^result19a <min>
          ^result19b <max>
          ^test-done 1)}

#run
#ctf expected/chunk19.soar print -cf