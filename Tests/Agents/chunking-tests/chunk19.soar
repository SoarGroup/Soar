# NCC tests

sp {apply20
    (state <s> ^name substate
               ^operator.name test
               ^superstate <ss>)
    (<ss> ^test-item { < <max> <min>}
          ^test-item <max>
          ^test-current 1)
         -{(<ss> ^test-item 20)}
-->
    (<ss> ^result20a <min>
          ^result20b <max>
          ^test-done 1)}