# Negative conditions

sp {apply21a
    (state <s> ^operator <o>
               ^superstate <ss>)
    (<ss> -^test-things
           ^test-current 1)
-->
    (<ss> ^result true ^test-done 1)
}

#run
#ctf expected/chunk20.soar print -cf