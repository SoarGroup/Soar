# Less than with two bindings
sp {apply5
    (state <s> ^name substate
               ^operator.name test
               ^superstate <ss>)
    (<ss> ^test-item <val>
          ^test-item { < <val> <var> })
-->
    (<ss> ^small5 <var> ^big5 <val>
          ^test-done 1)}

#run
#ctf expected/chunk5.soar print -cf