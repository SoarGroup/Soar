sp {answer*apply*query*success*2*snc*t3-3
    (state <s1> ^step 2)
    (<s1> ^superstate nil)
    (<s1> ^result <l1>)
    (<s1> ^result <l2>)
    (<l2> ^foo <c1>)
    -->
    (<s1> ^result2 <c1> +)
}

sp {answer*apply*query*success*1*snc*t3-1
    (state <s1> ^step 1)
    (<s1> ^superstate nil)
    -->
    (<s1> ^result <l1> +)
    (<l1> ^D Y +)
}

sp {answer*elab*query*success*2*onc*t3-2
    (state <s1> ^step 2)
    (<s1> ^superstate nil)
    (<s1> ^result <l1>)
    -->
    (<l1> ^foo bar +)
}
