sp {answer*apply*query*success*onc*t3-1
    (state <s1> ^superstate nil)
    -->
    (<s1> ^result <l1> +)
    (<l1> ^D Y +)
}

sp {answer*apply*query*success2*StateNoChange*t3-2
    (state <s1> ^superstate nil ^result <l1>)
    -->
    (<l1> ^foo bar +)
}
