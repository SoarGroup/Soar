# Negative conditions

sp {apply21b
    (state <s> ^operator <o>
               ^superstate <ss>)
    (<ss> ^test-item { > <y> <x> }
          ^test-item <y>
         -^foo { > <x> }
          ^test-current 1)
-->
    (<ss> ^result <y> ^test-done 1)
}

#run
#ctf expected/chunk21.soar print -cf