# Two bindings that can match the same thing

sp {apply10
    (state <s> ^name substate
               ^operator.name test
               ^superstate <ss>)
    (<ss> ^test-param <p1>
          ^test-param <p2>
          ^test-current 1)
-->
    (<ss> ^result10a <p1> ^result10b <p2>
          ^test-done 1)}

