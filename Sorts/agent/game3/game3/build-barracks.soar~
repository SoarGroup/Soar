sp {propose*build-barracks
   (state <s> ^name sorts
              ^running-task.name build-barracks
             -^build-building.started-build true)
-->
   (<s> ^operator <o> + > =)
   (<o> ^name build-building
        ^building-type 1 # see BuildFSM.h for building type->int translation
        ^building-dim 32) # width/2
}
