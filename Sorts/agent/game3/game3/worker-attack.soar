sp {propose*worker-attack
   (state <s> ^name sorts
              ^running-task <rt>)
   (<rt> ^name worker-attack
         ^id <id>)
-->
   (<s> ^operator <o> + =)
   (<o> ^name explore-attack
        ^unit-type worker
        ^group-size 5
        ^rush yes
        ^task-id <id>) 
}
