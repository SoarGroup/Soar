# it's better to use a unit that's already been singled
# out rather than to pick apart another group

sp {designate-explorer*prefer-existing-group
   (state <s> ^name designate-explorer
              ^operator <o1>
              ^operator <o2>)
   (<o1> ^name choose-explorer)
   (<o2> ^name isolate-explorer)
-->
   (<s> ^operator <o1> > <o2>)}