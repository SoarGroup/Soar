# to explore, we first have to decide which unit to use

sp {explore*propose*designate-explorer
   (state <s> ^name explore
             -^explorer)
-->
   (<s> ^operator <o>)
   (<o> ^name designate-explorer)}