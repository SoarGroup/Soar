To make this file:

  - Move the Interns directory into orts3/apps/
  - Go to the orts3 directory
  - Enter make interns

The general layout of this program is as follows:

interns_main:
   Event Handlers can be added to the agent here.

InternEventHandler:
   This is where updates to the game are handled,
   and decisions are made on which methods to execute.

SquadManager:
   Can place units into squads and manage individual squads.
   For right now it just tells squads where to move and calls
   the appropirate move method.

MoveManager:
   Contains the different types of move methods.

Squads:
   The class explaining how squads are organized.

InfluenceMap:
   Creates and updates the InfluenceMap.
