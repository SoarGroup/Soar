sp {go-to-location*propose*epmem-query
   (state <s> ^name go-to-location
             -^epmem.command.query
              ^superstate.operator <so>)
   (<so> ^name go-to-location
        -^id
         ^object-color <oc>)
-->
   (<s> ^operator <o> +, =, >)
   (<o> ^name epmem-query
        ^object-color <oc>)}

sp {go-to-location*apply*epmem-query
   (state <s> ^operator <o>
              ^epmem.command <ec>)
   (<o> ^name epmem-query
        ^object-color <oc>)
-->
   (<ec> ^query.waypoints.near-waypoint.sensed-object.color <oc>)
}